# Core Extensions
python-dotenv
flask
flask-pymongo
flask-mail
flask-limiter
flask-jwt-extended
flask-cors
gunicorn

# Database
pymongo[srv]
dnspython>=2.3.0
certifi

# ML / Image (Updated for Python 3.13)
numpy>=2.1.0
pillow
gdown

# PyTorch (CPU-only, version 2.6.0+ required for Python 3.13)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.6.0
torchvision>=0.21.0
torchaudio>=2.6.0